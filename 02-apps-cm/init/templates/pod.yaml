apiVersion: v1
kind: Pod
metadata:
    name: init-pod
    namespace: resumebuilder
spec:
    containers:
        - name: example-container
            image: nginx
            ports:
                - containerPort: 80